import React from 'react';
import {useLoaderData} from "react-router-dom"

const ShowDatials = () => {
    const service = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default ShowDatials;